# CodeMirror

这是代码编辑器 CodeMirror

```jsx
import { CodeMirrorWrapper, LangId, Theme } from 'x-star-design';
export default () => (
  <CodeMirrorWrapper
    lang={LangId.JAVA}
    theme={Theme.VSCODE}
    style={{ height: 500 }}
  />
);
```

## API

<API id="CodeMirrorWrapper"></API>
